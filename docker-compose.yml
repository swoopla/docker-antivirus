version: '2'

services:

  docker-av:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docker-av
    
    volumes:
      - ./data/queue:/data/av/queue
      - ./data/ok:/data/av/ok
      - ./data/nok:/data/av/nok
    
    networks:
      - avnetwork

networks:
  avnetwork:
